
internal partial class Program
{
    
        public static void MainArith(string[] args)
        {

        // with async type we can run both in parallel despite it being run on separate line
        //var taskserver = MyTask.SimpleArithServer();//MyTasks.SimpleServer();
       //var taskclient = Program.SimpleArithClient();

       //taskclient.Wait(); // wait for client to finish executing
        //taskserver.Wait();
 
        }
    }
